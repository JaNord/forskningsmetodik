# Title     : Project 1 for the course Forskningsmetodik för IT
# Objective : evaluate answers from SUS questionnaire

#loads .csv file into R session
answers <- read.csv("SUSsvar18och20.csv", header = TRUE, sep = ";")
